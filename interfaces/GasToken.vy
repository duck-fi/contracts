# Frees up to `_value` sub-tokens owned by address `_from`. Returns how many tokens were freed.
# You should ensure that you pass at least 25710 + `value` * (1148 + 5722 + 150) gas when calling this function.
@external
def freeFromUpTo(_from: address, _value: uint256) -> uint256:
    return 0
